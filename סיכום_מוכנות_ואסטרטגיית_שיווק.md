# סיכום מוכנות ואסטרטגיית שיווק - קורא PDF יומי

## האסטרטגיה שנבחרה

האסטרטגיה הנכונה למוצר היא לא לשווק אותו כ"תוסף בלבד", אלא כמוצר משולב:

1. **PWA מותקנת** - האפליקציה הראשית והאמיתית לקריאת PDF.
2. **תוסף Chrome/Edge** - שכבת נוחות, נראות ושיווק בתוך הדפדפן.

הסיבה: תוסף דפדפן רגיל לא יכול להפוך את עצמו לקורא ברירת המחדל של Windows לקבצי PDF. לעומת זאת, אפליקציית PWA עם `file_handlers` יכולה להירשם לפתיחת קבצי PDF כאשר הדפדפן והמערכת תומכים בכך, והמשתמש מאשר או בוחר אותה דרך Windows/Chrome/Edge.

לכן המסר השיווקי צריך להיות:

**קורא PDF עברי, מהיר ונוח לשימוש יומיומי - עם תוסף לדפדפן ואפשרות התקנה כאפליקציה.**

## מה כבר מוכן

### 1. גרסת Extension ל-Chrome/Edge

נוצר תוסף Manifest V3 בסיסי:

- `manifest.json`
- `extension/background.js`

התוסף:

- מופיע כאייקון בדפדפן.
- פותח את קורא ה-PDF בלחיצה.
- מוסיף אפשרות פתיחה דרך תפריט הדפדפן.
- משתמש בקורא הקיים ובכל הפונקציות שלו.

### 2. חבילת ZIP מוכנה להעלאה

נוצר סקריפט אריזה:

- `tools/build-extension.ps1`

הפלט:

- `dist/chrome-edge-extension` - תיקייה לטעינה ידנית ב-Developer Mode.
- `dist/daily-pdf-reader-extension.zip` - קובץ ZIP להעלאה לחנות.

### 3. התאמה לדרישות Manifest V3

הספריות החיצוניות הועברו לטעינה מקומית:

- `vendor/pdf.min.mjs`
- `vendor/pdf.worker.min.mjs`
- `vendor/pdf-lib.min.js`
- `vendor/html2canvas.min.js`

אין יותר תלות ב-CDN בזמן ריצה, וזה חשוב מאוד לאישור בחנות Chrome.

### 4. PWA קיימת ומוכנה כבסיס לאפליקציה מותקנת

קיים:

- `manifest.webmanifest`
- `service-worker.js`
- `file_handlers` לפתיחת קבצי PDF

ה-PWA היא החלק שאמור לשמש כאפליקציה עצמאית, כולל אפשרות עתידית לפתיחת קבצי PDF מהמחשב כאשר המשתמש מתקין ובוחר אותה.

### 5. אייקונים

נוצרו אייקונים חדשים ונקיים יותר שמתאימים לדפדפן ולחנות:

- `icons/icon-16.png`
- `icons/icon-32.png`
- `icons/icon-48.png`
- `icons/icon-128.png`
- `icons/icon.svg`
- `icons/maskable.svg`

האייקון הישן הוחלף כי טקסט קטן בתוך אייקון נראה פחות טוב בסרגל הדפדפן.

### 6. נכסי חנות מוכנים

נוצרה תיקייה:

- `store-assets`

בתוכה:

- 5 צילומי מסך בגודל `1280x800`
- תמונת קידום קטנה בגודל `440x280`
- תמונת Marquee בגודל `1400x560`

הקבצים:

- `store-assets/screenshot-01-reader-1280x800.png`
- `store-assets/screenshot-02-rtl-book-1280x800.png`
- `store-assets/screenshot-03-annotations-1280x800.png`
- `store-assets/screenshot-04-search-bookmarks-1280x800.png`
- `store-assets/screenshot-05-export-print-1280x800.png`
- `store-assets/promo-small-440x280.png`
- `store-assets/marquee-1400x560.png`

נוסף גם סקריפט ליצירה מחדש:

- `tools/generate-store-assets.ps1`

### 7. טקסטים שיווקיים

נוצרו קבצי טקסט לשימוש בחנות:

- `STORE_LISTING_HE.md`
- `STORE_MARKETING_COPY_HE.md`
- `STORE_ASSETS_README.md`

הקו השיווקי המרכזי:

**קורא PDF עברי, מהיר ונוח לשימוש יומיומי.**

דגשים:

- ממשק עברי זורם מימין לשמאל.
- מצב ספר RTL למסמכים בעברית.
- תמיכה גם במסמכים באנגלית.
- חיפוש, סימניות ושמירת מיקום קריאה.
- הדגשות, הערות, מילוי טפסים וחתימה.
- ייצוא PDF חדש עם התוספות.
- צילום עמוד, חלון או אזור מסומן.
- שמירה מקומית של מסמכים ונתונים בדפדפן.

### 8. מדיניות פרטיות

נוצר קובץ:

- `PRIVACY_POLICY_HE.md`

המסר המרכזי:

- התוסף לא שולח מסמכי PDF לשרת חיצוני.
- הנתונים נשמרים מקומית בדפדפן.
- ההרשאה `contextMenus` משמשת לפתיחה מהירה בלבד.

לפני פרסום בחנות צריך להעלות את מדיניות הפרטיות לכתובת URL ציבורית.

## מה עדיין צריך לעשות לפני פרסום

1. לבדוק ידנית את התוסף בדפדפן:
   - לפתוח `chrome://extensions` או `edge://extensions`.
   - להפעיל Developer Mode.
   - לבחור `Load unpacked`.
   - לטעון את `dist/chrome-edge-extension`.

2. לבדוק את הפעולות המרכזיות:
   - פתיחת PDF.
   - חיפוש.
   - סימניות.
   - הדגשות.
   - חתימה.
   - שמירת PDF.
   - הדפסה.

3. להעלות מדיניות פרטיות לכתובת ציבורית.

4. לפתוח או להשלים חשבון מפתח:
   - Chrome Web Store Developer Dashboard.
   - Microsoft Edge Partner Center.

5. להעלות את:
   - `dist/daily-pdf-reader-extension.zip`

6. להעלות את התמונות מתוך:
   - `store-assets`

7. להעתיק את הטקסטים מתוך:
   - `STORE_MARKETING_COPY_HE.md`
   - `STORE_LISTING_HE.md`

## ניסוח מומלץ לחנות

### תיאור קצר

קורא PDF עברי, מהיר ונוח עם סימניות, חיפוש, הדגשות, מילוי טפסים, חתימה ושמירת עותק PDF.

### תיאור שיווקי קצר

קורא PDF יומי נבנה בשביל עבודה אמיתית עם מסמכים בעברית: לפתוח, לקרוא, למצוא, לסמן, למלא, לחתום ולשמור בלי להסתבך.

הממשק עברי, ברור וזורם מימין לשמאל, עם מצב ספר RTL שמתאים במיוחד למסמכים בעברית, לצד תמיכה נוחה גם במסמכים באנגלית. הכלים החשובים נמצאים קרוב: סימניות, חיפוש, הדגשות, שדות טקסט, חתימות, צילום אזור מסומן, הדפסה וייצוא PDF חדש עם כל התוספות.

העבודה מתבצעת מקומית בדפדפן, והמסמכים האישיים נשארים אצל המשתמש.

## נקודת המיצוב

המוצר לא מתחרה רק ב"קוראי PDF כלליים", אלא מציע יתרון ברור:

**קורא PDF שמרגיש טבעי למשתמש עברי, עם זרימת עבודה יומיומית סביב קריאה, מילוי, חתימה ושמירה.**

זה הדגש שכדאי להבליט בצילומי המסך, בתיאור החנות ובכל חומר שיווקי.
